# Public backend config (safe to expose to the browser)
SUPABASE_URL=
SUPABASE_ANON_KEY=

# GEMINI_API_KEY is NOT stored here.
# Add it via Lovable Cloud (Supabase) -> Secrets so it stays server-side only.
